# This directory contains reusable Home Manager modules.
# Each module should be defined in its own file (e.g., `my-module.nix`).
#
# These modules are intended for sharing or for common configurations
# that can be imported across different Home Manager profiles.
# Refer to the NixOS Wiki for more information on modules: https://nixos.wiki/wiki/Module
{
  # Example: List module files here
  # my-module = import ./my-module.nix;
}
